// ─── Development Environment ──────────────────────────────────────────────────
// Used when running: ng serve / npm start (locally)
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3004/api',
  backendUrl: 'http://localhost:3004',
};
