// ─── Production Environment ───────────────────────────────────────────────────
// Used when running: ng build --configuration=production
// Automatically swapped in by Angular's fileReplacements config in angular.json
export const environment = {
  production: true,
  apiBaseUrl: 'https://zivorapetals-backend.onrender.com/api',
  backendUrl: 'https://zivorapetals-backend.onrender.com',
};
